## 1. Baseline Verification

- [x] 1.1 Verify `HMEngine` implementation matches `hm-engine` spec (registers, memory, metadata)
- [x] 1.2 Verify `ARCH_ISA` in `isa.py` matches `isa-v1` through `isa-v4` specs (opcodes, mnemonics, cycles)
- [x] 1.3 Verify `hmsim_cli` and `hmasm/hmdas` match `hmsim-cli` spec
- [x] 1.4 Verify `HMApplication` and its components match `hmsim-gui` spec

## 2. Finalization

- [x] 2.1 Sync all delta specs to main specs
- [x] 2.2 Archive the `initial-main-specs` change
